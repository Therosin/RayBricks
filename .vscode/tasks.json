{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start Game",
            "type": "shell",
            "command": "${workspaceFolder}/bin/raylib-lua-sol.exe",
            "args": [
                "${workspaceFolder}/main.lua"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ]
}
